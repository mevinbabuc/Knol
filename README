How to use
----------

```
docker-compose up -d
docker-compose exec web flask run --host=0.0.0.0 --port=5001
```

For the Front end, open the front_end/index.html in chrome.

also run the docker-compose for elastic search